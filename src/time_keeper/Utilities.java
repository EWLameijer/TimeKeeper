package time_keeper;

class Utilities {
	public static final String EOL = System.getProperty("line.separator");
	
	/** @@@[
	 * Aborts the program with an (error) message if some condition has not been 
	 * met, for example a required object is null.
	 * 
	 * @param condition
	 * @param message
	 */
	public void require(boolean condition, String message) {
	  
	}
}